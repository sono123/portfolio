

$(document).on('page:change', function() {

   $('img#ruby_logo').on('mouseenter', function(e){
      e.preventDefault()
      $(this).attr('src', '<%= asset_path("ruby_logo_hover.png")%>' )
   });

   $('img#ruby_logo').on('mouseleave', function(e){
      e.preventDefault()
      $(this).attr('src', '<%= asset_path("ruby_logo.png")%>' )
   });

   $('img#js_logo').on('mouseenter', function(e){
      e.preventDefault()
      $(this).attr('src', '<%= asset_path("js_logo_hover.png")%>' )
   });

   $('img#js_logo').on('mouseleave', function(e){
      e.preventDefault()
      $(this).attr('src', '<%= asset_path("js_logo.png")%>' )
   });

   $('img#html_logo').on('mouseenter', function(e){
      e.preventDefault()
      $(this).attr('src', '<%= asset_path("html_logo_hover.png")%>' )
   });

   $('img#html_logo').on('mouseleave', function(e){
      e.preventDefault()
      $(this).attr('src', '<%= asset_path("html_logo.png")%>' )
   });

   $('img#css_logo').on('mouseenter', function(e){
      e.preventDefault()
      $(this).attr('src', '<%= asset_path("css_logo_hover.png")%>' )
   });

   $('img#css_logo').on('mouseleave', function(e){
      e.preventDefault()
      $(this).attr('src', '<%= asset_path("css_logo.png")%>' )
   });

   $('img#sql_logo').on('mouseenter', function(e){
      e.preventDefault()
      $(this).attr('src', '<%= asset_path("sql_logo_hover.png")%>' )
   });

   $('img#sql_logo').on('mouseleave', function(e){
      e.preventDefault()
      $(this).attr('src', '<%= asset_path("sql_logo.png")%>' )
   });

});












